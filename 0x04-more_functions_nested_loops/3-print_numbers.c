#include "main.h"
/**
 * print_numbers - function to print numbers 0 to 9
 *
 * Return: returns 0
 */

void print_numbers(void)
{
	int x;

	for (x = 0 ; x < 10 ; x++)
		_putchar(x + '0');
	return (0);
}
